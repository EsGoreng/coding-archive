start
    input nilai

    if (nilai >= 60) then
        print "Lulus"
    else
        print "Tidak Lulus"
    EndIf

    //Menentukan kategori nilai
    if (nilai >= 90) then
        print("A")
    else if (nilai >= 80) then
        print("AB")
    else if (nilai >= 70) then
        print("B")
    else if (nilai >= 60) then
        print("C")
    else 
        print("D")
    EndIf
end 